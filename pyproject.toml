[project]
name = "ffe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "loguru>=0.7.3",
    "pybind11>=3.0.1",
    "setuptools>=80.9.0",
    "xarray>=2025.3.1",
]
